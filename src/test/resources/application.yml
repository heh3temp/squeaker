server:
    port: ${PORT:8082}
application:
    jwt:
        secretKey: ${SECRET_KEY:securesecuresecuresecuresecuresecuresecuresecuresecuresecuresecure}
        tokenPrefix: "Bearer "
        tokenExpirationAfterMinutes: 60
    squeaker:
        nickname: Squeaker
        email: squeaker@mail.com
        password: ${SQUEAKER_PASSWORD:password}
        description: Squeaking around 🐹

spring:
    datasource:
        url: jdbc:postgresql://${DATABASE_HOST:localhost}:${DATABASE_PORT:5432}/${DATABASE_NAME:sq_test}
        username: ${DATABASE_USERNAME:postgres}
        password: ${DATABASE_PASSWORD:postgres}
        driverClassName: org.postgresql.Driver

logging:
    level:
        com:
            hamsterbusters:
                squeaker: DEBUG